"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.default = void 0;var _config = await jitiImport("prisma/config");

// Encode special chars (e.g. ?) in the password segment so Prisma's URL parser doesn't choke
function fixUrl(url) {
  if (!url) return undefined;
  // Match protocol://user:password@host — password ends at the last @ before host
  return url.replace(/^([^:]+:\/\/[^:]+):([^@]+)@/, (_match, prefix, password) => {
    return `${prefix}:${encodeURIComponent(decodeURIComponent(password))}@`;
  });
}var _default = exports.default =

(0, _config.defineConfig)({
  schema: "prisma/schema.prisma",
  datasource: {
    url: fixUrl(process.env.DIRECT_URL ?? process.env.DATABASE_URL)
  }
}); /* v9-f2abe62ab4e13dd4 */
